# ── DairyPro 360 requirements ────────────────────────────────────────────────
# Works for both local dev (SQLite) and production (PostgreSQL on Render)

Django==4.2.13
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
argon2-cffi==21.3.0
python-decouple==3.8
django-filter==23.5

# Production server
gunicorn==21.2.0

# Static files (whitenoise serves them without a CDN)
whitenoise==6.6.0

# dj-database-url for future PostgreSQL support
dj-database-url==2.1.0
